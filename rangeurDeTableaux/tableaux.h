#include <stdio.h>
#include <stdlib.h>
void afficherTabl(int tabl[],int tail);
void ordonnerTableau(int tClasse[], int longTableau);
void echanger(int pi, int gi);
int userRestart(int reload);
void userLaunch();
